void funcion_backtracking (int *s, int slen, int pos, int n)
{
    int sum = 0;
    int iter = 0;

    
    7 3 2 1 4 
}

int main(int argc, char **argv)
{
    int n = atoi(argv[1]);
    int s[argc - 2] // -2 para descontar el nombre del programa y argv[1]

    int iter = 0;
    while(argv[iter] != NULL)
    {
        s[iter] == atoi(argv[iter + 2]); 
        iter++;
    }
    
    int slen = iter;
    iter = 0;
    while(iter != slen)
    {
        // funcion_backtracking(s, slen, iter, n);
        iter++;
    }
}